# 迭进 · 内容治理体检报告（收口版）

> 日期：2026-08-24 · 范围：运行版 `%CODEX_HOME%\.codex\diegin` → 本地源码库 → GitHub
> 原则：运行版是主体；源码库/Git 是同步副本；先看清、再收口、留痕可追溯。

## 一、结论摘要

1. **环境“散乱”问题已收口**：回收站清空、pycache 清理、备份机制核实、审计日志轮转、trail 归档、引擎漂移同步，六项全部落地并推送到 Git。
2. **数字口径已统一**：指标单一来源 `references/metrics_2026-08-24.json`；`DGEN STATUS 57` = 活跃规则，与 metrics `rules_active: 57` 一致；`rules_total 251` = 57 活跃 + 190 归档 + 4 其它状态，非数据矛盾。
3. **“残留垃圾”误判已纠正**：`*.pre_*` 是规则引擎 `_clean_old_backups(keep=5)` 的正常轮转备份（每类文件保留最近 5 份），不是垃圾；已加入 `.gitignore` 且核实清理逻辑完整（`_save_json` Step 6 每次保存后执行）。

## 二、已完成动作

| 项 | 动作 | 结果 |
|---|---|---|
| 回收站 | 31 个旧版残留/备份文件：先核验活体→生成留档清单 `references/archive/recycle_20260824_manifest.json`→清空 | 31 文件已删除（从未入 Git，留档可追溯） |
| pycache | 清理 2,240 个 `.pyc` / `__pycache__` | 目录已净 |
| 备份机制 | 核实 `rule_engine.py::_clean_old_backups` 与 `save_all` 脏标记 | 机制正常，保留最近 5 份 |
| .gitignore | 运行版补 `*.pre_*`，与源码库一致 | 已提交 `459fbf6` |
| 引擎漂移 | `pacemaker.py` DS 高峰窗口（08-20 拍板）+ `config.toml` 同步源码库 | 已提交 `a495650` |
| 审计日志 | 新增 `engine/_audit_rotate.py`（8MB/保留3份），接入 5 处写入入口；修复 `call_diegin` 时间戳重复写入；8.4MB 历史日志归档 `var/logs/archive/` | 已提交 `6e19f8d` |
| trail 流水账 | 13 个 `workspace/trail_*.md` 归档至 `references/archive/trails/` | workspace 仅剩 `dgen_rules.md` + `rule_health.json` |
| 指标单一源 | 白皮书/实战数据包/推广计划硬编码数字改为引用 metrics JSON | 已落地 |

## 三、当前环境结构（职责划分）

- **运行版**（`%CODEX_HOME%\.codex\diegin`）：主体。引擎代码 + 运行时状态（`var/state`、`var/logs`）+ 规则库（`engine/evo/rules`）+ 参考文档（`references`）。Git 不跟踪运行时状态。
- **本地源码库**（`%DEV_ROOT%\本地源码库\Diegin`）：引擎源码 + 文档的版本化副本。是“审推”门禁与推送的载体。
- **Git（GitHub `linsong-dev/diegin-skill`）**：只承载源码 + 文档；`.gitignore` 排除运行时/备份/日志。

## 四、已固化的预防机制

- 审计日志自动轮转（8MB/保留 3 份），防止单文件无限膨胀。
- 规则文件自动备份保留最近 5 份（引擎内置，已核实）。
- 指标数字单一来源（metrics JSON），发布文档不再硬编码。
- 引擎代码修改后统一走“运行版 → 源码库 → 审推 → Git”流程。

## 五、遗留决策项（P1/P2）

- **P2**：`references/archive/trails/` 旧流水账保留或按季度压缩？当前保留。
- **P2**：审计日志归档 `var/logs/archive/` 定期压缩为 `.zip`？当前保留明文。
- **P2**：`.pre_*` 备份在本地源码库不回传 Git（已 ignore）；如需异地容灾可另行评估。
- **P1**：网络推送依赖本地代理 `127.0.0.1:3067`；`.gh_token`（`gho_`）有效，`checkpush` 已内置“代理不可用→直连”降级，但直连在当前网络被 reset。推送建议保持开 VPN/代理。

## 六、与发布线路的关系

- 本次治理与「审推/发布」是两条线：治理线保证源码/文档干净一致；发布线（`platform-publish`）使用已审定的终稿。
- 待发布终稿（读后感+律令九章全文附录）已在 Git `ba6aadf`，明天由 `platform-publish` 发布。
