# 迭进工作记录 · 2026-08-05

## 攻七强化（v3.8）——正向经验优先复用

### Q1 及时使用
- pre_check：建议按置信度排序，conf≥4.5 且实质逻辑标 `priority`；display_line 升级 `✅ 攻七优先采用`
- pre_tool.ps1：输出独立醒目行 `✅ [迭进] 攻七·推荐优先采用`
- 验证：Bash 场景 `display_line: "[DGEN] PASS | ✅ 攻七优先采用: ..."`

### Q2 优先选用
- arbiter.resolve 新增 context 参数；P4 同场景加权：模式与当前工具/操作同场景 → 置信度 +0.5（不凌驾 P0-P3）
- main.arbitrate / pre_check 透传 context
- 验证：持平无 context→守三 block；同场景→攻七 allow（`同场景加成+0.5`）；异场景→守三 block（不误伤）

### Q3 泛化提速
- generalize_from_patterns 门槛：`tc≥3 且 conf≥3.0` → `tc≥2 或 conf≥4.5`（复用即验证信号）
- 验证：conf=4.5/tc=0 新模式单步泛化产出 `pat_rule_*`；5 个 active 模式均 auto_promoted 已收敛

### Q4 反馈闭环
- 新增 feedback_adopt 命令：adopted=true→record_user_feedback(agree) 置信度+0.5；false→veto ×0.7+override_count
- pre_tool 写 dgen_priority_pattern.json（推荐 pattern_id）；post_tool 工具成功自动采纳
- 修复 tracker veto 对 SuccessPattern 缺 override_count 字段崩溃（getattr 兜底）
- 验证：adopt 4.0→4.5、veto 4.5→3.15（文件级确认）；测试数据已清理

## 系统净化（防再生）

### 攻七空壳清理
- 新增 audit_patterns：空壳判定（decision_logic 空/<6 字符/无学习价值词/trigger 场景全空）→ archived
- 28 条空壳归档、保留 6 条实质模式；幂等验证（二次运行 archived=0）
- auto_sandwich：空壳被补全为实质内容 → 复活入 staging 验证门

### staging 积压清理
- 新增 audit_staging：源模式已归档/测试残留/超 14 天未触发 → archived；触发≥2 → active
- 23 条死亡 staging 归档、queue 26→3；自检 no_stale_staging

### 证据库去假阳性
- evidence_filter：active 规则放行不再写假 pass（存在≠验证）
- 新增 audit_evidence：429 条假证据标记 skip（reason 前缀 [伪]）
- 自检 no_fake_evidence

### health 真实化
- dashboard：熵=不稳定率（staging+deprecating+alerting+冲突）/总数（原恒 1.0）
- snr/satisfaction 无数据返回 -1 + [N/A]；recommendations 无数据不伪"正常"

### 一二不过三联动修复
- post_tool.ps1 条件倒挂：原 `-notmatch '"error"'` 检测到错误反而跳过记录 → 改为 `-match`（检测到错误才 record_error）
- 验证：command_failure 真实入库 strikes_db（测试后已恢复）

### 可观测性
- pre_check 落盘 [PACE]（缓急分流）与 [PHASE_LOCK]（封存命中）审计日志

## 回归实证
- 引擎自检 13 项全绿（dual_store_consistent、no_hollow_patterns、no_stale_staging、no_fake_evidence）
- test_all.py 16/16
- 双库一致：Mindol/JSON 255=255
- checkpush pre-check ALL CLEAN → push c6a5e56（已发布）
- 文档同步：SKILL.md / README.md / dgen_rules.md / dev_ops_sop.md / AGENTS.md / promotion_plan
