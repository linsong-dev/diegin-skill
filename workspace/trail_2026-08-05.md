# 迭进工作记录 · 2026-08-05

## 攻七强化（v3.8）——正向经验优先复用

### Q1 及时使用
- pre_check：建议按置信度排序，conf≥4.5 且实质逻辑标 `priority`；display_line 升级 `✅ 攻七优先采用`
- pre_tool.ps1：输出独立醒目行 `✅ [迭进] 攻七·推荐优先采用`
- 验证：Bash 场景 `display_line: "[DGEN] PASS | ✅ 攻七优先采用: ..."`

### Q2 优先选用
- arbiter.resolve 新增 context 参数；P4 同场景加权：模式与当前工具/操作同场景 → 置信度 +0.5（不凌驾 P0-P3）
- main.arbitrate / pre_check 透传 context
- 验证：持平无 context→守三 block；同场景→攻七 allow（`同场景加成+0.5`）；异场景→守三 block（不误伤）

### Q3 泛化提速
- generalize_from_patterns 门槛：`tc≥3 且 conf≥3.0` → `tc≥2 或 conf≥4.5`（复用即验证信号）
- 验证：conf=4.5/tc=0 新模式单步泛化产出 `pat_rule_*`；5 个 active 模式均 auto_promoted 已收敛

### Q4 反馈闭环
- 新增 feedback_adopt 命令：adopted=true→record_user_feedback(agree) 置信度+0.5；false→veto ×0.7+override_count
- pre_tool 写 dgen_priority_pattern.json（推荐 pattern_id）；post_tool 工具成功自动采纳
- 修复 tracker veto 对 SuccessPattern 缺 override_count 字段崩溃（getattr 兜底）
- 验证：adopt 4.0→4.5、veto 4.5→3.15（文件级确认）；测试数据已清理

## 系统净化（防再生）

### 攻七空壳清理
- 新增 audit_patterns：空壳判定（decision_logic 空/<6 字符/无学习价值词/trigger 场景全空）→ archived
- 28 条空壳归档、保留 6 条实质模式；幂等验证（二次运行 archived=0）
- auto_sandwich：空壳被补全为实质内容 → 复活入 staging 验证门

### staging 积压清理
- 新增 audit_staging：源模式已归档/测试残留/超 14 天未触发 → archived；触发≥2 → active
- 23 条死亡 staging 归档、queue 26→3；自检 no_stale_staging

### 证据库去假阳性
- evidence_filter：active 规则放行不再写假 pass（存在≠验证）
- 新增 audit_evidence：429 条假证据标记 skip（reason 前缀 [伪]）
- 自检 no_fake_evidence

### health 真实化
- dashboard：熵=不稳定率（staging+deprecating+alerting+冲突）/总数（原恒 1.0）
- snr/satisfaction 无数据返回 -1 + [N/A]；recommendations 无数据不伪"正常"

### 一二不过三联动修复
- post_tool.ps1 条件倒挂：原 `-notmatch '"error"'` 检测到错误反而跳过记录 → 改为 `-match`（检测到错误才 record_error）
- 验证：command_failure 真实入库 strikes_db（测试后已恢复）

### 可观测性
- pre_check 落盘 [PACE]（缓急分流）与 [PHASE_LOCK]（封存命中）审计日志

## 回归实证
- 引擎自检 13 项全绿（dual_store_consistent、no_hollow_patterns、no_stale_staging、no_fake_evidence）
- test_all.py 16/16
- 双库一致：Shalou/JSON 255=255
- checkpush pre-check ALL CLEAN → push c6a5e56（已发布）
- 文档同步：SKILL.md / README.md / dgen_rules.md / dev_ops_sop.md / AGENTS.md / promotion_plan

## 八元原则顺序统一（2026-08-05 下午）

- 认知顺序重排：**攻七 → 守三 → 一二不过三 → 举一反三 → 去伪存真 → 裁决律 → 缓急律 → 止观门**（原为裁决律 0 → 守三 1 → 攻七 2 …）
- 8 章节物理重排 SKILL.md（内容逐章比对无损）；README / AGENTS / promotion_plan 表格同步
- 每处加注：编号为认知顺序，执行优先级以裁决律 P0-P5 为准（真伪→安全→完形→紧急→置信→staging），不受编号顺序影响
- 新增「运行时主导映射表」（P0-P5 ↔ 主导原则），SKILL.md 裁决律章节 + README 各一份
- 发布：2b8bd72（顺序统一）+ 309a3fe（映射表）

## Hooks 修复（2026-08-05 下午）——攻七反馈闭环真实跑通

### 根因（auto_adopt 从未真正执行）
- post_tool.ps1 L149 `if (Test-Path $prioFile -and $toolExitCode -eq 0)`：`-and` 被 PowerShell 绑定为 `Test-Path` 的命名参数 → 每次抛 `A parameter cannot be found that matches parameter name 'and'`（审计 30+ 条 auto_adopt_error 印证）
- 更深一层：Codex PostToolUse hook 输入**不含 exit_code 字段**，`$toolExitCode` 恒 NULL → 即使修复 -and，`-eq 0` 也永不成立
- 结论：Q4 反馈闭环自写入起从未真正生效

### 修复
- 条件改为 `(Test-Path $prioFile) -and -not $toolError -and ($null -eq $toolExitCode -or $toolExitCode -eq 0)`（与 L307/L341「NULL 视为成功」约定一致）
- diegin_notify.ps1：补 UTF-8 BOM（消除 hooks_ps1_bom strike 根源）+ 修 `$d-and` 变量粘连（PS 解析为变量名 `d-and`）
- 验证：真实钩子触发 `[FEEDBACK-ADOPT] auto_adopt pattern=pat_auto_tool_Bash_1 result=...adopted=true`（真实模式自动采纳，priority 文件正确消费删除）

### Strike 清理（改毕验）
- dgen_overrides.json + 遗留 dgen_override.json：清除 hooks_ps1_bom 历史 strike（BOM 已全部修复）
- command_failure / tool_error_Bash（测试期语法错误记录）经 `verify_fix` 标记 verified → 经验固化攻七模式库
- 清理后 PreToolUse 不再出现 `[HOOK:OVERRIDE] BLOCK hooks_ps1_bom`

### 基线更新
- system_baseline.json → v2026-08-05：active 29→32（+3 条 shousan_review 防护规则：hooks_ps1_bom / command_failure / tool_error_Bash）、合计 260、patterns 37
- 自检 baseline_no_regression 回归绿

### 发布与状态
- 发布：9144aae（hooks 修复）+ 85ec1bf（基线更新）；origin/main = 85ec1bf
- 引擎自检：status ok、failed_checks []（13 项全绿）
- 说明：push 期间代理 127.0.0.1:3067 短暂中断，恢复后重试成功；var/state 整体 gitignore，仅 system_baseline.json 强制跟踪


## P2 防再生加固（2026-08-05 晚）——archived 复活根因修复 + checkpush 加固

### 观察期审计：工具名级伪模式污染攻七闭环
- pat_auto_tool_Bash_1（trigger="tool_name == 'Bash'"、conf=5.0、触发 11165+ 次）被 auto_adopt 116 次无意义 +0.5、pre_check 每次 Bash 都推荐 → 空壳无学习价值
- 处置：归档 + call_diegin.py 建议生成增加工具名级噪音过滤（_tool_lvl_noise = trigger 以 "tool_name ==" 开头；_is_priority 需 conf>=4.5 且 logic>=6 且非噪音）

### 复活根因（决定性排查）
- 表面现象：归档后下次真实钩子运行 Shalou 变回 active → 一度怀疑 generalize_cross_domain / record_success / review 链路
- 决定性实验：归档后跑真实 post_tool 钩子（含 generalize 触发）→ 三态保持 archived，排除钩子链路
- 真相：**多进程内存缓存竞争**。旧进程内存缓存仍为 active，其任意 update_pattern（哪怕只改 confidence）都会把内存全量状态写回 Shalou(SQLite INSERT OR REPLACE)+JSON，覆盖归档 → 复活
- 佐证：Shalou add_unit 幂等（uid 主键），不存在多版本单元；引擎启动 Shalou 为权威源（_load_all 优先 _load_from_shalou）

### 修复（3 文件）
- rule_engine.py update_pattern：
  1) archived→active/staging 需显式 _force_reopen=True，否则拒绝（防任何未来代码路径自动复活）
  2) 未显式改状态时，从 Shalou(SQLite) 直读权威 status 纠正内存陈旧值，防旧进程全量覆盖（JSON+Shalou 双保护）
  3) Shalou 写入 status 用权威值而非内存旧值
- rule_engine.py add_pattern：同 id 幂等去重（防重复模式堆积）
- main.py auto_sandwich：archived 模式直接跳过强化（不 +0.3、不补全、不 promote）
- call_diegin.py audit_patterns：新增「工具名级伪模式」判定（decision_logic 为纯工具名/标识符且无 trigger_condition → 归档），防同类再产生

### 归档与验证
- 归档 3 个工具名级伪模式：pat_auto_tool_Bash_1 / pat_auto_tool_list_mcp_resource_templates_1 / pat_auto_tool_get_goal_1
- 验证：真实钩子后三态 archived；旧进程模拟 update_pattern(confidence) 不覆盖权威；无 _force_reopen 复活被拒；_force_reopen=True 人工可复活；auto_sandwich 跳过 archived（tc 不变）；audit_patterns 幂等（archived 0/kept 9 无误伤）
- test_all.py 16/16；引擎自检通过（265 规则 / 40 模式）；active 模式 8 个全部有实质决策逻辑

### P2b 双库一致性
- Shalou/JSON：规则 265=265、模式 39=39，无重复 id、状态 0 不匹配

### P3 checkpush 加固（本地工具，非 git 仓库）
- GBK 防崩溃：stdout/stderr reconfigure UTF-8+replace；git 子进程二进制捕获+UTF-8 解码；commit 消息改 UTF-8 临时文件 -F（含 emoji/特殊字符不再崩）
- 代理检测：_proxy_ok() 探测 127.0.0.1:3067，不可用自动降级直连
- API 重试：5xx/网络错误指数退避 3 次 + JSON 解析容错；push 失败重试 3 次
- 验证：py_compile、GBK 中文+emoji 输出、代理检测、git 子进程、token 读取均正常

### 发布
- Diegin commit 9453c98 → push 成功（cf58844..9453c98 main -> main，token 注入 URL）
- checkpush 本地生效（非 git 仓库不推送）
## 补充核查（2026-08-05 晚）——规则库同步遗漏发现与修复
- 完成审计时发现源码库 interception_rules.json 落后运行版（260 vs 265）：上轮 P2 同步仅覆盖 success_patterns.json
- 差异明细：新增 4 条真实防护规则（shousan_review_tool_error_Bash / pat_rule_gongqi_fix_tool_error_Bash / shousan_review_command_failure / pat_rule_gongqi_fix_command_failure）
- 同时发现并清理测试残留：pat_rule_pat_test_dup_guard（trig=t，由 add_pattern 幂等测试的模式泛化衍生）→ 已从 JSON+Shalou+内存三态删除，规则库 265→264
- 状态文件合并去重同步：evidence_trail 运行版201+源码库473=674（无丢失）；dgen_archive 运行版1240（含源码库443全量）
- success_patterns.json 逐字段对比 0 差异（仅键序/格式差异，无需同步）
- 发布：4268e8d；origin/main=HEAD、ahead=0、工作区干净
- 结论：无其他待办（剩余 DIFF 均为运行时状态文件 dgen_archive/evidence_trail/last_maintenance 的常态演进差异）

## P2 接手（2026-08-05 深夜）——基线更新 + 假证据再生修复 + _force_reopen 审计留痕

### 交接待办执行
- system_baseline.json 更新：rules_total 265→264、rules_staging 9→8、frozen_at 刷新；自检 baseline_no_regression 绿
- 防再生长周期验证：无 _force_reopen 复活被拒（返回 False）、_force_reopen=True 可复活并还原、旧进程改 confidence 不覆盖权威、三态（内存/JSON/Shalou）一致 archived、3 个工具名级伪模式保持 archived
- pre_check 推荐质量验证：工具名级噪音 0 推荐；命中场景（stdin+json）推荐 pat_ps_stdin_bom_flow priority=True conf=5.0 实质逻辑；缓急律宕机时段行为确认（23:00-06:00 一切 downtime，测试时间无关化）

### 新发现与修复（自检 FAIL 驱动）
- 根因：evidence_trail 出现 14 条 evidence_filter 假 pass（全部来自已删除测试残留 pat_rule_pat_test_dup_guard），自检 no_fake_evidence FAIL
- 修复 1（call_diegin.py）：evidence_filter 对 staging 阈值达标改写 skip（「staging阈值达标…evidence_filter批量非验证」），从源头不再产生 pass（防再生）
- 修复 2（call_diegin.py）：audit_evidence 匹配条件由死字符串「active规则已通过验证」改为任意 evidence_filter+pass（原匹配词与实际写入的「staging规则通过验证」不匹配，清理永久失效）
- 修复 3（rule_engine.py）：_shalou_warn 与新增 _audit_reopen 的审计日志路径错误（engine/evo 下 `..\var\logs` 解析到不存在的 engine\var\logs，静默丢弃）+ 模块缺 import io 导致写盘 NameError 被吞——两者叠加使 guard 告警从未落盘；补 import io + 路径改 `..\..\var\logs`，guard 拒绝与 _force_reopen 复活现均可审计留痕
- 修复 4（test_all.py）：test_pacemaker 在宕机时段（23:00-06:00）必然 FAIL（flaky），测试内屏蔽 _check_downtime 使 16/16 时间无关

### 验证与发布
- 自检 13/13 全绿（fake_evidence_count=0、dual_store_consistent、baseline_no_regression）
- test_all.py 16/16
- 发布 409f646（4 文件：call_diegin.py/rule_engine.py/test_all.py/system_baseline.json）；origin/main=HEAD、ahead=0、工作区干净

### 观察项（未动，供后续决策）
- active 的 6 个 gongqi 模式 trigger 为 op_contains(X)，_safe_evaluate 白名单拒绝非 Attribute Call → 表达式永不命中；shousan_review 同类规则靠 Shalou 语义回退浮出，但攻七模式无语义回退 → 攻七推荐对 tool_error/command_failure 场景实际不触发（仅 pat_ps_stdin_bom_flow 类可表达式命中的模式会被推荐）。是否给 _safe_evaluate 增加 op_contains 语义需人工裁决。
## Shalou 全量同步权威保护（2026-08-06）——「覆盖重置」残余风险收口
- 用户追问：Shalou 是否真的解决了「被覆盖重置」→ 全代码核查所有写 Shalou 路径
- 结论分层：
  - 基础层安全：Shalou core `_load()` 只读、`add_unit` 单条幂等（INSERT OR REPLACE）、`save()` 仅 commit——**无整库 DELETE+重建**
  - 引擎单条路径安全：`update_pattern/add_pattern/update_interception` 已由 P2a 权威状态纠正+复活护栏覆盖
  - **残余风险**：`_shalou_sync_all()` 全量同步（`_load_all` fallback + `save_all` 数量不一致两个入口）会用当前进程内存全量覆盖已存在单元 → 旧进程陈旧 active 可覆盖权威 archived
- 补强：`_shalou_sync_all(force_status=False)` 构建权威状态快照，**单向保护**——仅当权威 archived 且内存 active/staging 时以权威为准（防旧进程复活）；新归档（内存 archived）正常传播；新增规则正常写入；`force_status=True` 为显式全量重建入口
- 验证（隔离临时库 4 场景）：SCEN-A 防复活✅ SCEN-B 归档传播✅ SCEN-C 新增写入✅ SCEN-FORCE 显式重建✅；test_all 16/16；自检通过（264 规则/39 模式）
- 发布：ea63d1d（基于 d6b0867，含新对话 409f646 假证据再生修复）

## P2 续（2026-08-06 凌晨）——op_contains 谓词落地（8元原则复核后方案A实施）

### 背景
- 上轮发现 active 的 6 个 gongqi 模式 trigger 为 op_contains(X)，_safe_evaluate 白名单拒绝非 Attribute Call → 表达式永不命中；守三同类靠 Shalou 语义回退、攻七无回退 → 攻七/守三/一二不过三三链残缺
- 按 8 元原则逐条复核方案 A（实现 op_contains 谓词）通过，附带去伪存真 3 条硬约束：单字符串常量参数白名单 / 字段白名单可审计 / 有效性回归验证

### 修复（rule_engine.py + test_all.py）
1. `_safe_evaluate` 注入 op_contains 谓词：字段白名单（blocked_error_type/op/error_type/type/cmd/text）+ token 长度≥3 + 仅子串命中；`prechecked` 默认 False（tracker 规则语义：未预检）
2. `_safe_evaluate` Call 检查放行 op_contains(单字符串常量)，其他调用形态一律拒绝（含撞字段名如 op_contains(error_type)）
3. **NOT 标准化修复**：_safe_evaluate/_analyze_trigger_fields 缺 `\bNOT\b -> not`，大写 NOT 导致 ast.parse SyntaxError → 所有带 NOT 的规则（op_contains(X) AND NOT prechecked）永不命中
4. `_match_condition` 快速路径 ops 加 'op_contains('（否则被当纯关键词子串匹配）
5. `_analyze_trigger_fields` 跳过 op_contains 参数 Name（字面 token 非字段引用，消除 P0 误报）
6. `_BUILTIN_FIELDS` 加 op_contains/prechecked（字段审计已知集合）
7. HOOK_CONTEXT_TEMPLATES 加 tool_error_Bash 错误场景模板（消除 op_contains 规则 P2 误报）
8. test_all.py 新增 test_op_contains 7 用例

### 验证
- 单元 15/15：命中 blocked_error_type/op/cmd 连字符/带引号/OR 组合/AND NOT prechecked/真实 pre_tool 模板；无关字段/短 token/空参数/撞字段名拒绝
- 端到端：tool_error_Bash 上下文 → 守三 5 + 攻七 2，winner=gongqi_verified（✅ 攻七优先采用）；hooks_ps1_bom → 攻七 priority 推荐；command_failure → 守三 block 优先（P1 安全）；普通任务 0 命中（无误伤）
- test_all 23/23；自检 13/13（dual_store 264=264、no_fake_evidence、no_bareword_true_rules）；3 个工具名级伪模式三态 archived
- 发布 2ec9fc8；期间另一会话已推 ea63d1d（_shalou_sync_all 权威保护，运行版已合体），rebase 后源码库与运行版 SHA 一致，origin/main=HEAD