# 迭进 trail 2026-08-06

## Mindol 检索状态过滤（2026-08-06 晚）——上下文噪音收口
- 背景：pre_check 的 mindol_context 会注入已归档规则/模式的文本（如 pat_rule_pat_auto_tool_Bash_1 status=archived），造成上下文噪音
- 排查：检索链路 memory_search → CodexMemoryAdapter.search → core.retrieve；rule_engine.retrieve_for_task 直连 retrieve 已有 active 二次过滤（无需改）
- 修复：codex_adapter.search 多取候选（top_k*4）→ 过滤 rule/pattern 空间中 status=archived 单元 → 取 top_k；聊天等非 JSON 文本放行（保留可追溯，仅检索层过滤）
- 验证：pre_check mindol_context 仅含 active 规则（archived leak=0）；memory_search/search CLI 无 archived 命中；test_all 23/23
- 发布：12d38ff（origin/main 同步）