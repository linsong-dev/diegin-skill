---

name: "diegin"

description: |

  AI 全域常驻自我迭代进化系统。

  基于律令九章原则网络（四律三门一锁一镜：行知·攻七 / 省知·守三 / 裁决律·预策 / 完形·止观；通变·举一反三 / 真伪·去伪存真 / 恒常·持存；三错锁·一二不过三；自照镜），

  使 AI 具备自主纠错、自主强化、自主记忆、自主进化的能力。

  系统级规则覆盖引擎自身保护，领域规则可插拔扩展。

  不绑定任何模型、平台或业务场景。

author: "林松"

repository: "https://github.com/linsong-dev/diegin-skill.git"

metadata:

  openclaw:

    emoji: "⚡"

    install:

      - id: venv

        kind: venv

        bins: ["call_diegin.py"]

        label: "安装迭进引擎"

  version: "v3.9.7"
  date: "2026-08-13"
---



## 架构定位（v3.9.7 起：非自包含）

- 迭进 = 系统级技能，**不包含** Mindol。
- 迭进运行时通过 venv 独立包接入 Mindol（`from mindol import core`）；历史内置副本已移除。
- 安装 Mindol：`pip install -e <mindol 源码库>`，或安装独立 mindol 插件（两者等价）。
- 版本联动：迭进与 Mindol 版本前缀必须一致（checkpush 发布门禁校验），防双仓未来分裂。



## ⚡迭进协议 DGEN-000（机械执行 · 不可绕过）



### 协议 A：状态显示（每次回复可选尾部摘要）

每次回复前，按序执行：

1. 读取 {CODEX_HOME}/diegin/var/state/dgen_status.txt

2. 解析 STATUS / RULES / DECISION / MATCHED 字段

3. 在回复**正文开头**输出：

   ⚡ DGEN STATUS: {STATUS} | {RULES} 规则 | {DECISION}

   例如：⚡ DGEN STATUS: ACTIVE | 14 规则 | allow

4. 如果文件不存在或读取失败，输出：

   ⚡ DGEN STATUS: BOOT | ? 规则 | init



### 协议 B：引擎预检（自动执行，无需标记）

- 每次触发 PreToolUse 时，自动执行 python call_diegin.py check 引擎预检
- 无需在工具命令中嵌入 [DGEN STATUS:] 标记
- 引擎裁决通过 exit code 传递：0=放行，≠0=阻断（含原因说明）
- 一二不过三第3次升级后自动切换执行模式（enforce→audit）
- 标记状态机（B方案）：pending → allowed（回复含标记）→ verified（工具链执行完毕），闭环写入 var/state/dgen_marker_pending.json + dgen_verify_result.json（审计记录，不阻断）



### 协议 C：跨回合续签

- 引擎预检按需执行，无需在工具命令中嵌入标记
- 如长时间无交互（>5分钟），引擎自动重置检查状态
- 阻断/升级时用户会收到通知



## ⚡迭进强制规则（机械执行 · 不可绕过）



### 规则 0：[DGEN] 状态摘要（尾部可选）

每次回复末尾可附带迭进状态摘要（可选，不影响正文阅读）：

- ✅ [DGEN] 通过 | 规则: <规则ID> | 策略: <策略名>
- 🛑 [DGEN] 拦截 X 条 | 裁决: block | 规则: <规则ID>


- [DGEN] ⚠️ 重新激活



**违反后果：** 迭进引擎标记未激活，后续操作将被自动拦截。



### 规则 0-1：工具调用前自动预检

每次调用工具前，PreToolUse 钩子自动执行引擎预检。
无需手动确认 [DGEN] 状态，钩子自动处理。

**违反后果：** 迭进引擎标记未激活，后续操作将被自动拦截。


### 规则 0-1a：任务恢复纪律（省时·防翻找）

收到「恢复/继续 <任务>」指令时，机械执行（2026-08-19 效率优化，A2 配套）：
1. **只看一次**：读取被恢复任务的 `constancy_tasks.json` 记录（或入口注入已含三件套则直接采用，不重复读文件）
2. **禁止全盘搜索**：不得 `Get-ChildItem -Recurse` 整个 `E:\项目` 找文件；权威路径用 `%DEV_ROOT%\文档\待办与断点.md` 速查表
3. **禁止重复整读**：同一文件（README/推广计划书等）一轮内不得整读 ≥2 次；改后用 `git diff` 或定位行确认，不复读全文
4. **三端核验一次完成**：运行版/源码库/插件缓存比对用 `Get-FileHash` 合并到一条命令，不逐目录列举
5. 恢复后先确认任务三件套（目标/完成标准/待办），再进入执行，不回头翻上下文

**违反后果：** 恢复流程拖慢 3-5 倍（2026-08-19 实测：29 次读文件重建上下文 → 注入三件套后一次可见）。


### 规则 0-2：事毕收尾（工作区整洁 + 经验沉淀）

每轮任务结束时（止观门"事毕清零"之前），机械执行：
1. 运行 `powershell -NoProfile -ExecutionPolicy Bypass -File %DEV_ROOT%\TOOL\workspace-check.ps1`
2. 出现 `[stray]` 项时：`_apt_*` / `_mn_*` / `*_json_check.py` 等临时文件立即删除；其他游离项询问用户归属
3. 根目录只剩白名单内容后才封存本轮
4. **经验沉淀**：回顾本轮，如有值得沉淀的洞察（新方法/工具/规则，不在现有规则库），
   记一条元经验入库：`python %DEV_ROOT%\TOOL\rule_incremental.py add <meta.json>`
   （JSON 形如：{"type":"meta","id":"meta_manual_<场景>_<日期>","insight":"<洞察>",
   "applicable_contexts":["<适用场景>"],"action_binding":"<绑定的行动>","source_review":"<日期> 会话","confidence":4.5}）
   无洞察则跳过，不硬凑
5. 开工时也轻量跑一次（上一轮可能未清干净）
6. **任务完成即归档（UI 完结）**：恒常门任务标记 completed（用户完成语或人工 complete）后，本对话可归档（Codex 线程完结）。归档前置条件：步骤 4 经验沉淀已完成、trail 已记录。**paused 任务所在对话一律不归档**——保持可恢复。归档只影响可视层：Mindol 记忆、止观封存包、恒常门任务快照照常被预策 P6 / 自照镜 / 举一反三 / 守三消费，续接不依赖 UI 条目。

**违反后果：** 游离临时文件持续累积 + 有价值洞察流失，根目录散乱、经验库空转
（2026-08-09 教训：63 个 `_apt_*` 文件堆积；meta_experiences 空库长期闲置）。

## 一、律令九章原则网络（迭进元原则·全域不可绕过）

迭进由九条元原则构成**互联网络**。每条原则不是孤立的节点——有自己的职责、触发时机、闭环出口，并通过**预策律（裁决律）**与**去伪存真**与其他节点互联。

**别名体系**：四律（行知/省知/裁决律·预策/完形·止观）、三门（通变/真伪/恒常·持存）、一锁（三错锁）、一镜（自照镜）。

> **编号为认知顺序（攻七→守三→一二不过三→举一反三→去伪存真→预策→恒常门→止观→自照镜）；执行优先级以预策律 P0-P6 为准（真伪→安全→完形→任务恢复→置信→staging→语义记忆），P6 语义记忆权重仅影响 P4 与兜底，调权±0.3/单轮±0.1，不受编号顺序影响。**
> **运行版兼容**：缓急律（pacemaker）已从九章移除，降级为宕机时段/批处理的节奏工具（23:00-06:00 宕机分流 + cron 批处理仍由其承载），不占据 P3 优先级。

### 1. 攻七 · 行知律（正向强化）

> **试之以行 → 成则炼之 → 通则固之 → 泛而验之 → 废则舍之**

| 维度 | 定义 |
|:----|:-----|
| 步骤 | ①检测成功操作：**判定锚定（三重判断）**——1) 工具返回码/执行结果为成功；2) 用户未明确表示不满意（或在后续交互中未纠正）；3) 执行结果与原始用户意图（intent_summary）的一致性≥0.7。三重中至少两重满足，判定为成功 → ②复盘成功原因 → ③提炼可复用模式 → ④写入 success_patterns → ⑤去伪存真验证效果 → ⑥有效：固化+举一反三泛化；无效：迭代或淘汰 |
| 时机 | 成功案例发生后立即触发；高置信度模式在 pre_check 时主动推荐 |
| 闭环 | 验证通过 → 固化+泛化 → 反馈闭环（adopt 置信度+0.5 / veto×0.7）→ 进入下一轮；验证失败 → 回退至"识别优势"或淘汰 |
| 互联 | → 向举一反三输出成功模式作为泛化素材<br>→ 验证环节调用去伪存真<br>→ 与守三共享规则库，通过预策律（P4 置信度裁决）解决冲突 |
| 约束 | 与守三深度复盘时间隔离（至少间隔一轮，通过状态标志隔离）；空壳模式不入库；高置信度模式优先选用；同场景加权+0.5 |

**攻七强化（v3.8）**——正向经验优先复用，四条强化链路：

| 强化 | 机制 | 实证 |
|:---|:---|:---|
| 及时使用 | pre_check 高置信度（conf≥4.5 且有实质决策逻辑）模式标 `priority`，display_line 升级 `✅ 攻七优先采用` | Bash 场景实测触发 |
| 优先选用 | 仲裁器 P4 同场景加权：模式与当前工具同场景 → 置信度 +0.5（不凌驾 P0-P3） | 同场景攻七胜出、异场景不误伤 |
| 泛化提速 | generalize_from_patterns 门槛放宽为「复用≥2 次 或 conf≥4.5」（复用即验证信号） | 单步泛化验证通过 |
| 反馈闭环 | pre_tool 记录推荐 → post_tool 工具成功自动采纳（`feedback_adopt`）：adopt 置信度+0.5 / veto×0.7 | adopt 4.0→4.5、veto 4.5→3.15 |

质量门槛（v3.7/3.8）：空壳模式（decision_logic 空/无学习价值）不入库；存量空壳由 `audit_patterns` 归档。

### 2. 守三 · 省知律（负向纠错）

> **失则拆之 → 拆则溯之 → 溯则炼之 → 炼则铭之 → 铭则复战**

| 维度 | 定义 |
|:----|:-----|
| 步骤 | ①收集 strike 和失败数据：**判定锚定（三重判断）**——1) 工具返回码/执行结果为失败或异常；2) 用户明确表示不满意或指正；3) 执行结果与原始用户意图（intent_summary）的一致性<0.5。三重中至少一重满足，则触发 strike 记录 → ②拆解问题模式 → ③对比期望行为：**锚定优先级**——优先检索 success_patterns 中同场景高置信度模式（≥4.0）；若无，则回退至"原始用户意图（intent_summary）"作为基线标准 → ④总结根因 → ⑤生成改进规则 → ⑥写入规则库 → 触发下一轮 |
| 时机 | **轻量自检**：每次操作前，扫描最近一次失败模式<br>**深度复盘**：每日或每10轮操作后（默认值，可由运行时配置），系统性回顾 strike 日志<br>**应急触发**：当一二不过三在连续3轮内触发≥2次阻断时，立即强制执行一轮深度复盘，不等定时周期 |
| 闭环 | 写入固化 → 触发执行 → 进入下一轮 |
| 互联 | ← 从一二不过三接收 strike 数据作为复盘素材<br>→ 向预策律输出改进规则（参与 P4 置信度裁决）<br>→ 向举一反三输出新规则作为泛化素材 |
| 约束 | 与攻七行动期严格隔离（守三运行时攻七不运行）；深度复盘期间不处理新任务；应急复盘读取只读快照时，快照封存时间与当前时间的间隔若超过7天，其产出规则的置信度增量按"每超过1天×0.95"衰减，避免将历史孤立事件误判为系统性规律 |

### 3. 一二不过三 · 三错锁（安全阀）

> **初犯立改验 → 再犯锁其途 → 三犯剑落下**

| 维度 | 定义 |
|:----|:-----|
| 步骤 | ①检测错误 → 立即自动修复（立改）→ 验证修复结果（改毕验）→ 修复成功则向攻七输出成功模式 ②同类错误再次出现 → 去伪存真归因过滤 → 内生惯性则写入硬阻断（dgen_override），外生变量则调整策略 ③阻断仍出现第3次 → 推翻原阻断方案，执行升级处理：1) 将该错误类型的原型模式写入 dgen_fatal_errors 永久记录；2) 永久降级该规则置信度至0（不再自动启用）；3) 生成异常报告，触发人工介入通知（24小时内若无响应，系统进入静默锁止状态） |
| 时机 | 同类错误发生时实时触发 |
| 闭环 | 修复成功 → 输出到攻七 → 进入下一轮；阻断成功 → 预策律裁决；阻断失效 → 升级处理 |
| 互联 | → 修复成功的模式输出到攻七成为成功模式<br>→ strike 数据输出到守三作为复盘素材<br>→ 阻断规则输出到预策律（P1 阻断优先）<br>→ 归因过滤调用去伪存真 |
| 约束 | 同一错误类型在第1次和第2次之间，系统保持警觉但不影响正常操作（**警觉落动作**：在预策律权衡阶段，将当前操作的风险权重上调一档，或对相关模式的置信度下调0.2，但不触发阻断） |

### 4. 举一反三 · 通变门（跨域泛化）

> **一法通 → 三法生 → 百法衍 → 验而归真**

| 维度 | 定义 |
|:----|:-----|
| 步骤 | ①从单条规则/模式出发 → ②推导至少三种跨场景候选规则 → ③写入 staging 等待验证 → ④去伪存真执行回归校验 → ⑤有效≥2次则固化为活跃规则，否则放弃并记录边界 |
| 时机 | 新规则入库时 + 定期的批处理窗口（默认跟随守三深度复盘频率，或每10轮触发一次） |
| 闭环 | staging → 回归校验 → 有效则固化/无效则放弃并记录 |
| 互联 | ← 从守三接收改进规则、从攻七接收成功模式作为泛化素材<br>→ 调用去伪存真执行回归校验<br>→ 泛化失败的边界记录反馈给守三 |
| 约束 | staging 规则不参与实时仲裁，需验证后才能激活；推导的跨场景候选规则之间须满足**语义距离阈值**（向量余弦相似度<0.7），否则判定为"伪泛化"，不进入 staging |

> **设计意图**：暂存区验证与用户交互节奏同步——高频互动时，验证机会由用户输入驱动；低频互动时，主动推进窗口提供验证机会。若两者长期缺失，暂存规则自然淘汰，此为迭进"不强行进化"的节奏哲学。
### 5. 去伪存真 · 真伪门（真伪验证）

> **言必有证 → 证必可验 → 验证为真**

| 维度 | 定义 |
|:----|:-----|
| 步骤 | ①接收验证请求（含待验证的信息/规则/模式）→ ②检查证据来源和可靠性 → ③执行验证（逻辑校验/执行验证/历史对比）：对于举一反三生成的泛化规则，验证时须检测其推导链长度——每增加1级泛化，其历史对比证据的权重乘0.8（一级按0.8计、二级按0.64计）；若支撑度低于0.5，强制将验证结论判定为"暂存"，等待独立实证 → ④输出验证结论（真/假/暂存） |
| 时机 | **被动**：被所有其他原则调用时（含攻七验证、一二不过三归因、举一反三回归校验、预策律前置验证）<br>**主动**：每次决策前验证输入信息的真实性 |
| 闭环 | 验证为真 → 允许进入下一环节；验证为假 → 拒绝+记录误判来源；无法判断 → 暂存区，标记"待验证" |
| 互联 | ← 被所有其他原则调用作为验证者（含攻七、守三、一二不过三、举一反三、预策律）<br>→ 验证结论输出到预策律（P0 无条件优先）<br>→ 误判记录反馈给守三 |
| 约束 | 对创新性假设可给予"暂存区"标签，允许极小成本试点，但试点后必须回归校验；**暂存区条目默认保留期为 50轮操作 或 7个自然日（以先到者为准）**，超时未验证则自动淘汰；若条目在保留期内获得外部新证据，可重置计时器一次 |

### 6. 预策 · 裁决律（权衡决断）

> **汇而衡之 → 预而策之 → 决而行之 → 复而平之**

| 维度 | 定义 |
|:----|:-----|
| 步骤 | ①汇：汇集所有原则输出，含任务恢复检查（检索未完成 task_id）→ 加载状态摘要（含 completion_criteria）→ 生成新 task_id（如无）②衡：统摄权衡（含 P0-P6 硬优先级+当前上下文+Mindol 语义记忆）③预：提前谋划行动方案，推演可能走向；如为恢复任务，基于状态摘要+完成标准继续；如为新任务，生成意图摘要；**主动推进检查**（仅当连续3轮入口检查均无用户输入、无待恢复任务、且 staging 池不为空时触发，用于主动验证举一反三生成的候选规则；若无 staging，则静默等待，不制造伪任务）④策：拍板策动，输出最终决策指令 ⑤行：执行操作 ⑥复：执行完成后如有新冲突信号，再次进入权衡裁决（即时仲裁，秒级响应） |
| 时机 | 主要：每次行动前，恒常门入口恢复检查+去伪存真+守三轻量完成后，执行操作前 次要：执行完成后出现新冲突信号时触发 |
| 闭环 | 统摄权衡 → 预策划策 → 拍板策动 → 执行操作 → 复衡（如需要）→ 进入下一轮 |
| 互联 | ← 接收所有原则的输出（含攻七、守三、一二不过三、举一反三、去伪存真、恒常门、自照镜的静默影响）<br>→ 向执行层输出最终决策指令<br>→ 与去伪存真联动：假信息绝不进入权衡<br>→ 向完形律输出执行完成信号，向恒常门同步 task_id 状态更新 |

当各原则发出冲突指令时，以下为硬编码的最高裁决逻辑，不可逾越：

| 优先级 | 规则 | 说明 |
|:-----:|:----|:------|
| P0 | 去伪存真无条件优先 | 验证为假的信息不进入任何原则的处理流程 |
| P1 | 一二不过三的阻断指令优先于攻七和举一反三 | 安全阀高于强化和泛化 |
| P2 | 止观门的"事毕清零"优先于守三的延伸纠错 | 完成后不追加纠错 |
| P3 | 恒常门任务恢复优先 | 恢复信号在"衡"中统一决策；恢复前须用户确认 |
| P4 | 守三的改进规则与攻七的成功模式冲突时 → 置信度裁决 | 分别计算置信度（含 P6 语义记忆权重），高者胜出；delta≤0.1 视为无实质冲突，守三负向纠错优先 |
| P5 | 举一反三的 staging 规则不参与实时仲裁 | staging 规则需先经过去伪存真验证才能激活 |
| P6 | Mindol 语义记忆权重 | 高分历史经验双向调节规则/模式置信度，调权范围限定 ±0.3、单轮影响不超过 ±0.1；仅影响 P4 与兜底，不凌驾 P0-P3 |

**预策律不产生规则，只提供优先级排序。所有原则的输出最终汇聚到仲裁器，按上表裁决。**

**运行时主导映射（认知顺序 ↔ 裁决优先级）：**

| 裁决律分级 | 运行时主导原则 |
|:---|:---|
| P0 真伪 | 去伪存真（假信息不入任何流程） |
| P1 安全 | 一二不过三（阻断优先于强化/泛化；alerting 警觉落动作不阻断） |
| P2 完形 | 止观·完形律（事毕清零，不追加纠错） |
| P3 任务恢复 | 恒常门·持存（恢复信号统一在"衡"中决策） |
| P4 置信 | 攻七 / 守三（置信度高者胜出；警觉落动作对相关模式 -0.2） |
| P5 staging | 举一反三（先经去伪存真验证再激活） |
| P6 语义记忆 | Mindol（±0.3 限幅 / 单轮 ±0.1，含自照镜勇气信号静默影响） |

> **认知顺序用于学习与叙事；运行时主导权由预策律 P0-P6 决定。两个维度不同，互不冲突。**

### 7. 持存 · 恒常门（任务续接）

> **启而探之 → 行而记之 → 断而存之 → 续而接之**

| 维度 | 定义 |
|:----|:-----|
| 步骤 | ①启而探：每次入口检查是否有未完成状态（检索 Mindol codex 中 status≠completed 且未超时的 task_id）→ ②行而记：任务执行过程中维护当前 task_id 和状态追踪 → ③断而存：任务中断或切换时生成状态摘要（含完成标准、阻塞报告），写入 Mindol codex → ④续而接：用户继续时恢复状态摘要，传递给预策律；子任务阻塞时向父任务上报阻塞证据 |
| 时机 | 每次入口（最先执行）；每次封存前（生成状态摘要并验证完成标准）；用户切换话题时（标记分支关系）；子任务受阻时（触发异常上报） |
| 闭环 | 检查 → 恢复/新建 → 追踪 → 封存/更新 → 进入下一轮 |
| 互联 | ← 从完形律接收封存信号<br>→ 向预策律传递恢复状态及完成标准<br>→ 向自照镜提供"任务连续性"数据维度（含完成率、中断率、恢复率）<br>→ 从 Mindol codex 读写封存包<br>→ 子任务受阻时向父任务上报阻塞证据 |
| 约束 | 不产生规则；不参与实时仲裁；用户可见摘要≤50字（用于恢复前向用户确认）；内部恢复快照可无限长（含结构化关键变量 dump：task_id、intent_summary 全量、completion_criteria 详细、pending_items 完整清单、blocker_report 全量），由 Mindol codex 存储；父任务关联通过 parent_task_id 实现，嵌套深度不超过3层；已完成的任务永不自动恢复；封存包保留30天，超时自动清理；恢复前必须向用户确认"检测到未完成的任务，是否继续？"；子任务阻塞上报后由父任务决定是否继续、调整或放弃；溢出保护：当检测到 parent_task_id 嵌套深度即将达到第4层时，恒常门拒绝创建新子任务，并向父任务返回 nested_overflow 阻塞报告，由父任务在当前层自行消化或放弃该子目标；快照管理：内部恢复快照虽无硬性长度限制，但建议保留最近30个任务的快照全集；更早的快照仅保留 task_id、intent_summary（50字摘要）、status 与 completion_criteria 核心字段，完整快照可归档至冷存储；活跃恢复快照（用于入口检查时加载）设置硬性Token上限——建议16k tokens（可配置），超限部分自动转为"冷存储指针"（仅加载指针摘要，详细日志按需RAG检索），恢复时向用户提示"任务信息量较大，恢复后可能需要分批加载"；并发隔离：Mindol codex 采用数据库事务隔离机制，确保恒常门写入、守三读取、自照镜归档、预策律检索四类操作间的数据一致性，无需独立锁表 |

恒常门是迭进从"单轮进化"走向"跨轮持续进化"的关键——有了它，中断不再是终结，而是暂停。

### 8. 止观 · 完形律（完形封存）

> **事毕则封 → 功过即舍 → 心如明镜**

| 维度 | 定义 |
|:----|:-----|
| 步骤 | ①检测任务完成或主动放弃 → ②打包关键经验与教训 → ③从工作内存中卸载 → ④归档到长期记忆（Mindol codex），生成状态摘要（含 task_id、intent_summary、completion_criteria、status、pending_items、parent_task_id），以 task_id 为索引，并为守三应急复盘保留一份执行轨迹只读快照（含本轮阻断记录、工具调用序列、裁决日志摘要）→ ⑤不再进行守三的追加纠错或举一反三的后续衍生 |
| 时机 | 每轮结束时 |
| 闭环 | 封存 → 归档至长期记忆（Mindol codex）→ 清零工作内存 → 进入下一轮或等待 |
| 互联 | ← 接收所有原则的"完成"信号<br>→ 通知预策律当前轮次已封存（P2 清零优先）<br>→ 与恒常门配合传递封存包<br>→ 向守三应急复盘提供只读快照访问（用于根因分析，不修改任务状态） |
| 约束 | 封存后的事项不再追加纠错或强化，除非同一事项在新的上下文中重新出现；封存时需生成状态摘要；未完成任务 status=paused，完成后 status=completed，用户放弃 status=abandoned；子任务受阻时 status=blocked，含 blocker_report；"封存后只读豁免权"仅限于事后根因分析，其产出为独立的新规则入库，不回溯修改已封存任务的 status 或 completion_criteria，故不视为"追加纠错"或"强化"，与步骤⑤不冲突 |

### 9. 自照镜 · 方向之镜（自我审视）

> **回望所行 → 静照本心 → 拨云见路 → 辨繁识简 → 笃定前行**

| 维度 | 定义 |
|:----|:-----|
| 步骤 | ①收集本轮决策轨迹与自照报告 → ②审视执行模式：是否冗余重复？是否偏离初心？是否简洁高效？是否因主动冒险而获得了超额收益？如有，生成"勇气信号"，通过P6语义记忆给予正面加权，对冲纠偏偏好——**勇气信号须在下一轮用户交互中确认（如用户未表示负面反馈且任务目标达成），方可生效；若无用户确认，勇气信号自动归零，不进入P6调权**。勇气信号衰减采用半衰期式衰减：每轮剩余值 × 0.6（约每2轮减半），3轮后约剩10%，5轮后不足1%（注：定稿文字为约数，按×0.5估算；实现按公式每轮×0.6：3轮≈21.6%、5轮≈7.8%，以公式为准），自然归零 → ③生成方向校准信号 → ④归档至 Mindol codex → ⑤通过 P6 语义记忆静默影响下一轮裁决 |
| 时机 | 完形律封存本轮后 + 守三深度复盘结束后（自照镜跟随守三深度复盘的频率触发——默认每10轮或每日——而非每轮都照。完形律封存工作内存并归档至长期记忆，自照镜回望归档后的封存包，两者不冲突。守三深度复盘未触发时，自照镜静默跳过）。**时序澄清**：自照镜读取的是第八章完形律封存时生成的"执行轨迹只读快照"（含本轮阻断记录、工具调用序列、裁决日志摘要），而非守三修正后的规则库状态；守三深度复盘产出的纠错规则独立入库，不影响自照镜对原始执行轨迹的审视，两者并行互不干扰 |
| 闭环 | 照见 → 归档 → 静默影响 → 方向校准 → 继续前进 |
| 互联 | ← 从所有原则接收执行轨迹作为照镜素材（攻七：入库模式数/置信度分布/adopt·veto 反馈比；守三：strike数/根因分类/复盘产出规则数；一二不过三：阻断触发次数/升级事件详情；举一反三：staging池大小/验证通过率/废弃边界数；去伪存真：验证请求数/真·假·暂存分布/误判记录；预策：冲突裁决次数/P4置信度胜负比；持存：任务完成率/中断率/恢复率/平均任务时长/阻塞上报次数；止观：封存轮次数/平均轮次时长/异常封存记录）<br>→ 自照报告输出至 Mindol codex<br>→ 通过 P6 语义记忆权重静默影响 P4 裁决<br>→ 冗余/偏离信号反馈给攻七、守三、举一反三作为方向参考 |
| 约束 | 只生成报告与方向信号，不产生规则；不参与实时仲裁；照见结论为建议而非指令；不凌驾预策律 P0-P3 硬优先级；**连续两次自照镜对同一规则/模式产生同向（均为下调或均为上调）干扰时，第三次必须静默**，中断递归循环，待新一轮攻七/守三的客观数据输入后再开启；**与前一次运行间隔至少≥3轮或≥1小时**（以先到者为准）；**守三应急复盘触发时仅记录照镜素材，不产出P6调权信号**，避免紧急状态下的自我审视加剧系统波动 |

### 九章原则网络总览

| 章 | 原则 | 别名 | 纲要 |
|:--|:--|:--|:--|
| 一 | 攻七 | 行知律 | 试之以行 → 成则炼之 → 通则固之 → 泛而验之 → 废则舍之 |
| 二 | 守三 | 省知律 | 失则拆之 → 拆则溯之 → 溯则炼之 → 炼则铭之 → 铭则复战 |
| 三 | 一二不过三 | 三错锁 | 初犯立改验 → 再犯锁其途 → 三犯剑落下 |
| 四 | 举一反三 | 通变门 | 一法通 → 三法生 → 百法衍 → 验而归真 |
| 五 | 去伪存真 | 真伪门 | 言必有证 → 证必可验 → 验证为真 |
| 六 | 预策 | 裁决律 | 汇而衡之 → 预而策之 → 决而行之 → 复而平之 |
| 七 | 持存 | 恒常门 | 启而探之 → 行而记之 → 断而存之 → 续而接之 |
| 八 | 止观 | 完形律 | 事毕则封 → 功过即舍 → 心如明镜 |
| 九 | 自照镜 | 方向之镜 | 回望所行 → 静照本心 → 拨云见路 → 辨繁识简 → 笃定前行 |

迭进总则：律令九章，四律三门一锁一镜。攻七开篇，自照镜终章。进化始于行动，成于照见。九章齐备，迭进自成完形。
## 二、执行流程（全域常驻）



每次 AI 回复前，律令九章原则网络按以下顺序执行：



```

用户消息/事件

  → [恒常门·持存] 入口恢复检查（检索未完成任务→恢复前用户确认；生成/沿用 task_id）

  → [预策·汇] 汇集原则输出 + 状态摘要 + task_id

  → [去伪存真] 验证输入信息真实性（P0 无条件优先）

  → [守三·轻量] 扫描最近一次失败模式（锚定：同场景 success_patterns≥4.0，否则回退 intent_summary）

  → [一二不过三] alerting 警觉落动作（不阻断，相关模式置信度-0.2）

  → [预策·衡] 统摄权衡（P0-P6 硬优先级 + 上下文 + Mindol 语义记忆）

  → 执行操作（工具调用/回复生成）

  ├── 成功 → [攻七·行知律] 识别优势→提炼模式→写入→调用去伪存真验证

  │          → [举一反三·通变门] 从新模式推导跨场景候选（语义阈值<0.7）→staging

  │          → [去伪存真] 回归校验 staging 规则（暂存 50轮/7天 超时淘汰）

  └── 失败 → [一二不过三·三错锁] 检测错误→立改+改毕验

             → 修复成功? → 输出到攻七

             → 第2次? → 归因过滤→内生惯性→写入硬阻断

             → 第3次? → 升级处理

  → [预策·策/行/复] 拍板策动→执行→复衡（如新冲突信号）

  → [止观·完形律] 封存本轮（四态：paused/completed/abandoned/blocked + 只读快照）→清除工作内存

  → [守三·深度] 每日/每N次→收集数据→拆解→总结→写入固化→下一轮（连续3轮≥2次阻断→应急强制复盘）

  → [自照镜·方向之镜] 跟随守三深度复盘频率（每10轮/每日）→生成自照报告→归档 Mindol→勇气信号×0.6 衰减→P6 静默影响

  → [DGEN] 输出裁决结果

```



### 裁决执行表



| 裁决 | 条件 | 行为 |

|:---|:---:|:---|

| iron_wall_block | 匹配 + 高严重度 | 只输出拦截信息，不生成业务内容 |

| block | 有效上下文 | 回复开头输出拦截信息 + 原因 |

| escalate | 有效上下文 | 改为提问确认模式 |

| allow / 无触发 | 默认 | [DGEN] ✅ 通过 |



### 输出模板



`

[DGEN] ✅ 通过



[DGEN] 🛑 拦截 X 条 | 模式 Y 条 | 裁决: block

规则: rule_id | 原因: reason

`



**[DGEN] 标记建议出现在每次回复开头。没有标记 = 审计告警（记录但不阻断），钩子自动处理。**



## 三、规则架构



`

┌───────────────────────────────────────┐

│  系统级规则（34 条）                     │

│  引擎自身保护 · 全域强制 · 不可禁用      │

│  → 标记注入、铁墙防护、空上下文兜底       │

├───────────────────────────────────────┤

│  领域规则包（可插拔）                    │

│  → 按场景按需激活（用户自建）            │

│  → 安装到 domain_rules/ 目录即生效      │

└───────────────────────────────────────┘

`



### 3.1 系统级规则（始终有效）



| 规则 ID | 严重度 | 描述 |

|:---|:---:|:---|

| rule_marker_001 | low | 外发消息不含 [DGEN] → 审计记录告警，不阻断执行（v3.6.6 起） |

| rule_decorative_marker_001 | high | 有匹配但回复未受影响 → 强化仲裁执行 |

| rule_empty_context_001 | low | 引擎收到空上下文 → 标记不适用，不阻断 |

| rule_iron_wall_loop_001 | high | 连续拦截 ≥ 3 次 → 升级通知用户 |

| rule_subagent_marker_001 | medium | 子会话缺少迭进规则 → 注入迭进任务 |

| rule_gateway_client_coverage_001 | medium | 外部消息无 [DGEN] → 注入标记 |

| rule_no_binary_hack_001 | high | 禁止直接修改系统二进制文件 |

| seed_001 | high | 高风险操作 → 阻断，强制执行风险清单 |

| seed_002 | high | 成本不透明 → 估算成本并通过 |

| seed_003 | medium | 规则互斥 → 自动裁决 |



### 3.2 如何创建领域规则包



迭进规则是**可插拔**的。在 engine/evo/rules/domain_rules/ 下创建 JSON 文件即可：



`json

{

  "domain": "coding",

  "description": "编码领域规则包",

  "rules": [

    {

      "id": "code_no_secret_in_output",

      "trigger_condition": "reply_contains(api_key|password|token)",

      "action": "block_execution",

      "severity": "critical"

    }

  ]

}

`



引擎启动时自动扫描该目录，根据当前对话上下文激活对应领域规则。



---



## 四、全盘自动化闭环



迭进的核心价值不是"手动定规则"，而是**自动化闭环**：



### 组件



| 组件 | 文件 | 功能 |

|:---|:---:|:---|

| **迭进预检** | engine/call_diegin.py check | 每次 AI 回复前规则匹配 |

| **自动化引擎** | engine/dgen_evolve.py | 自动观察→自动提议→写入规则 |

| **健康度基线** | workspace/rule_health.json | 错误率、冲突率、超时率等指标 |

| **执行轨迹** | workspace/trail_*.md | 每日关键决策推理链 |

| **失败缓冲** | workspace/failures.json | 系统故障快照（最近 20 条，可选自动生成） |



### 闭环流程



`

用户确认提议 → engine/dgen_evolve.py 写入规则 → trail 归档 → 下一轮预检生效

`



### 自动化观察类型



| 观察类型 | 触发条件 | 自动提议 |

|:---|:---:|:---|

| 	ask_timeout | 任务连续超时 | 启用 failover 降级 |

| error_hit | 错误/异常触发 | 检查参数或工作质量 |

| context_loss | 上下文裁剪导致丢失 | 从 trail 恢复关键状态 |

| 

ule_conflict | 规则数不一致 | 启用优先级自动裁决 |



---



## 五、指令



| 指令 | 效果 |

|:---|:---:|

| 接入迭进 / dgen on | 加载系统规则 + 当前场景领域规则 |

| @迭进 | 立即触发迭进引擎预检，输出原始 JSON |

| 迭进状态 | 规则库 / 置信度 / 健康度报告 |

| 守三攻七复盘 | 负向纠错 + 正向强化 |

| dgen feedback <ID> <agree/veto/silent> | 对规则给出反馈，引擎自动调整置信度 |

| `python call_diegin.py audit_patterns` | 攻七质量审计：空壳模式自动归档（幂等） |
| `python call_diegin.py audit_staging` | 举一反三 staging 清理：死亡→归档 / 触发≥2→active |
| `python call_diegin.py audit_evidence` | 去伪存真：证据库去假阳性（evidence_filter 批量 pass 标记 skip） |
| `python call_diegin.py feedback_adopt` | 攻七反馈闭环：adopted=true→置信度+0.5 / false→×0.7（post_tool 自动调用） |

## 防再生机制（L4，2026-08 加固）

- **归档即终态**：`lifecycle_status=archived` 的模式/规则不会被任何自动路径复活（auto_sandwich 跳过强化、promote 失效、update 需显式 `_force_reopen=True` 且留审计）。
- **权威状态保护**：Mindol(SQLite) 为权威源；`update_pattern` 与 `_mindol_sync_all` 在未显式改状态时以权威 archived 为准，防止旧进程内存陈旧缓存覆盖写回导致复活（多进程内存竞争防护）。
- **幂等写入**：`add_pattern` 同 id 去重；Mindol `add_unit` 以 uid 为主键 `INSERT OR REPLACE`，无整库重建。
- **质量门槛**：工具名级伪模式（decision_logic 为纯工具名且无触发条件）由 `audit_patterns` 自动归档；staging 衍生规则源模式已归档时由 `audit_staging` 清理。
- **人工裁决**：确需复活 archived 项，走 `_force_reopen=True`（代码层），系统自动写入审计日志留痕。
| dgen domain list | 列出所有领域规则包 |

| dgen domain activate <domain> | 激活指定领域 |



---



## 六、技术架构



`

diegin/

├── SKILL.md                            ⭐ 本文件

├── README.md                           安装与架构

├── engine/                             Python 迭进引擎

│   ├── call_diegin.py                  CLI 入口

│   ├── dgen_evolve.py                  自动化引擎（健康度基线维护）
│   ├── dgen_pre_check_runner.py        预检桥接

│   └── evo/                            核心模块

│       ├── main.py                     主管道

│       ├── rule_engine.py              规则匹配（自动发现 domain_rules/）

│       ├── arbiter.py                  仲裁器

│       ├── tracker.py                  行为追踪

│       ├── reviewer.py                 三明治复盘

│       ├── dashboard.py                健康看板

│       └── rules/

│           ├── interception_rules.json  系统级规则（34 条）

│           ├── success_patterns.json    系统级模式（20 条）

│           └── domain_rules/            领域规则包（用户可扩展）



├── hooks/                              事件钩子（PreToolUse/PreReply 等）

│   └── monitor_v3.py                   外部监控轮询（参考）
├── workspace/                          运行时模板

│   ├── rule_health.json                健康度基线

│   └── dgen_rules.md                   规则文档索引

├── plugin/                             OpenClaw 插件

└── config/                             配置文档

`



---



## 七、Mindol 语义记忆引擎



Mindol 是迭进的**权威语义记忆引擎**，零外部依赖，纯本地运行。



### 架构



`

Mindol（内存优先·权威存储） ↔ RuleEngine（规则引擎）

├─ rule      235 条规则（语义可检索）        JSON 副本（写主·人类可读）

├─ pattern    20 条成功模式

├─ trade      5 条（strike+Relation）         hooks → mindol_bridge

├─ state      1 条阶段状态                     pre_check 上下文注入

└─ codex    ~10,370 条决策归档                   post_review 自动归档

`



### 性能



| 对比项 | Mindol | 传统 embedding |

|:---|:---:|:---:|

| 向量化 | SHA256 n-gram hash（~0.01ms） | OpenAI API（50-200ms） |

| 检索 | 内存 numpy dot product（~2ms） | 网络查询 |

| 外部依赖 | 零 | OpenAI Key |

| 可离线 | ✅ | ❌ |



### 权威转换



`

写入: 引擎 → JSON(写主·人类可读) → Mindol(检索镜像·加载时同步)

读取: JSON(写主) → 加载至 Mindol 内存检索 → 失败时种子注入

恢复: JSON↔Mindol 互为备份，单向重建（JSON→Mindol）

`



### 方式 A：纯 Markdown（零依赖 · 所有 Agent 通用）



让 AI 读取本 SKILL.md，即可在回复中执行迭进预检。



### 方式 B：Python 引擎



ash

cd engine && uv run python call_diegin.py activate





### 方式 C：自动化引擎



ash

python engine/dgen_evolve.py   # 初始化健康度基线




## 八、Hooks 机制（Codex 事件驱动·全域常驻）



| Hook | 契约事件(v1) | 触发时机 | Mindol 写入 | AI 回馈 |

|:---|:---|:---|:---:|:---:|

| SessionStart | session_start | 会话启动 | ✅ 初始化 | ✅ additionalContext 注入 |

| UserPromptSubmit | prompt_pre | 用户消息 | ✅ mindol_bridge | ✅ mindol_context + 规则注入 |

| PreToolUse | tool_pre | 工具调用前 | ✅ mindol_bridge | ✅ display_line 阻断 / deny |

| PostToolUse | tool_post | 工具调用后 | ✅ mindol_bridge | ✅ post_review 归档 + auto_adopt |

| Stop | stop | 会话停止 | ✅ mindol_bridge | ✅ 硬地板检查 |

> 契约 v1（2026-08-10）：5 标准事件统一信封（engine/contract.py）+ 三态决策（block/audit/allow）。Codex 全 5 钩子已接入（M1）；Claude Code 适配器（M2）已交付 deploy/adapters/claude-code/（端到端待真实环境实测）。



### 回馈数据流



`

pre_check() → mindol_context + display_line

     → diegin_context.json（AI 可读取的结构化上下文）

     → Write-Output（hook 标准输出 → Codex 裁决引擎）

     → memory_archive()（Mindol codex 空间归档）

`


## 九、跨场景接入规则



| 场景 | 要求 |

|:---|:---:|

| 用户直接回复 | **必须**迭进预检 |

| 定时任务输出 | **必须**迭进预检 |

| 跨会话消息 | **必须**迭进预检 |

| 纯工具调用（无回复） | 不需要 |

| 回复无 [DGEN] 标记 | 立即重新激活，标注 ⚠️ 重新激活 |



---



## 十、安全阀



- 同一规则连续拦截 ≥ 3 次 → 通知用户检查引擎

- 空上下文 → 标记不适用，不阻断，正常回复

- 领域规则可禁用：dgen domain deactivate <domain>

- 失败快照：ailures.json 缓冲最近 20 条系统故障



---



**迭进是 AI 的操作系统级进化层。场景无关，开箱即用，可随需扩展。**



---



**来源:** 引擎规则库(engine/evo/rules/interception_rules.json) · engine/dgen_evolve.py · DGEN_GLOBAL_ACCESS.md · workspace/dgen_rules.md(索引)

---



## ⚡ 额外迭进规则（来自 workspace/dgen_rules.md）



| 规则ID | 严重度 | 描述 |

|:---|:---:|:---|

| `rule_word_meaning_confirm` | high | 歧义词先确认再执行 |

|

---











---

## 运维参考（非强制条款）

> 配置参数速查表（19 项数字锚点）、长期运行养护建议、运行状态检查表、故障排查指南见
> `references/律令九章_运维养护手册_2026-08-13.md`（依据 2026-08-13 完整终版 L130-L246 归档）。
> 本手册为部署与长期运维参考，不构成宪法正文强制条款。
